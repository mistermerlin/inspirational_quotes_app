import 'package:flutter/material.dart';

class AppColors {
  static const primaryColor = Colors.orange;
  static const accentColor = Colors.black;
  static const quoteBackgroundColor = Colors.white;
  static const shadowColor = Colors.grey;
}
